{% extends 'base.html.twig' %}

{% block title %}choix de catégorie{% endblock %}

{% block body %}


<div class="container">
        <div class="row mt-5">
        <h6></h6>
        </div>
                <div style="border: 1px solid">
                <h1 class="my-1" style= "text-align : center">Catégorie : {{categorie.name}} </h1>
                </div>
                {% if retex|length <= 0 %}
                <div class="row mt-5">
                <h6></h6>
                </div>
                                       

                        <div class="alert alert-dismissible alert-secondary">
                       
                        <h4 class="alert-heading"></h4>
                        <p class="mb-0">Il n'y a pas de production enregistrée pour cette catégorie</p>
                        </div>
                {% else %}

                        <div class="row mt-5">
                                
                                    {% include 'categorie/_categorie.html.twig' %}
                        </div>

                       
                {% endif %}
                             
                             
</div>
{% endblock %}