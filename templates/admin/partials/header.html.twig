<nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">RTL!</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

                <div class="collapse navbar-collapse" id="navbarColor01">
                            <ul class="navbar-nav mr-auto">

                                <li class="nav-item">
                                    <a class="nav-link" href="{{path ('admin_dash_board')}}">
                                    <i class="fas fa-chart-line"></i>
                                    Dashboard
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{path ('admin_retex_index')}}">
                                    <i class="far fa-file-alt"></i>
                                    Publication
                                    </a>
                                </li>

                              
                                 <li class="nav-item">
                                    <a class="nav-link" href="{{path ('admin_user_index')}}">
                                    <i class="fas fa-users"></i>
                                    Utilisateurs
                                    </a>
                                </li>

                            </ul>
                            
                                
                                
                           
                            <ul class="text-center">
                            <h4 class=""> ADMINISTRATION</h4>
                            </ul>
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                        <a href="{{ path ('homepage') }}" tagert="_blank" class="nav-link">
                            <i class="fas fa-eye"></i>
                            voir le site
                        </a>
                        </li>
                        
                       {% if app.user.Filename %}
                                <a href="#" class="nav-link dropdown-toggle" data-toggle = "dropdown" id="accountDropdownLink">
                                <img src="{{  asset('images/users/' ~ app.user.Filename) }}" class="avatar avatar-mini" alt="Avatar de {{app.user.fullName}}">
                                </a>  
                                 <div class="dropdown-menu dropdown-menu-right" aria-labellby="accountDropdownLink">
                                <a href="{{ path ('admin_account_logout') }}" 
                                class="dropdown-item">Déconnexion!</a>
                                </div>
                                {% else %}
                                <a href="#" class="nav-link dropdown-toggle" data-toggle = "dropdown" id="accountDropdownLink">
                                <i class="fas fa-user-tie">  {{app.user.fullName}}</i></a>
                               
                                 <div class="dropdown-menu dropdown-menu-right" aria-labellby="accountDropdownLink">
                                <a href="{{ path ('admin_account_logout') }}" 
                                class="dropdown-item">Déconnexion!</a>
                                </div>

                            </li>
                       {% endif %}
                    </ul> 
                                           
                    
                </div>
</nav>
                {% for label, messages in app.flashes %}
                    <div class="container">
                    <div class="alert alert-{{ label }}">
                        {% for message in messages %}
                        <p>{{ message | raw }}</p>
                        {% endfor %}
                    </div>
                </div>
                {% endfor %}