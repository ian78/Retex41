<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href={{ path('homepage') }}>RTL!</a>
     
            {% if app.user %}
                <div class="collapse navbar-collapse" id="navbarColor01">
                 
                            <ul class="navbar-nav mr-auto">
                                <li class="nav-item">
                                <a class="nav-link" href="{{path ('categorie')}}">Catégories</a>
                                </li>
                                
                            
                                <li class="nav-item">
                                <a class="nav-link" href="{{path ('redacteurs')}}">Rédacteurs</a>
                                </li>
                                
                            </ul>

                            <ul class="navbar-nav ml-auto">                    
                            <li class="nav-item dropdown">
                                {% if app.user.Filename %}
                                <a href="#" class="nav-link dropdown-toggle" data-toggle = "dropdown" id="accountDropdownLink">
                                <img src="{{  asset('images/users/' ~ app.user.Filename) }}" class="avatar avatar-mini" alt="Avatar de {{app.user.fullName}}">
                                </a>  
                                {% else %}
                                <a href="#" class="nav-link dropdown-toggle" data-toggle = "dropdown" id="accountDropdownLink">
                                <i class="fas fa-user-tie">  {{app.user.fullName}}</i></a>
                                {% endif %}
                            <div class="dropdown-menu dropdown-menu-right" aria-labellby="accountDropdownLink">
                            <a href="{{path ('retex_newone')}}" class="dropdown-item">Rédigez un compte rendu</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{ path ('account_index' , {'slug' : app.user.slug}) }}" class="dropdown-item">Mon compte</a>
                            <a href="{{ path ('account_profile'  )}}" class="dropdown-item">Modifier mon profil</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{ path ('account_logout') }}" class="dropdown-item">Déconnexion!</a>
                            </div>
                            </li>
                        
                        {% else %}
                            <div class="collapse navbar-collapse" id="navbarColor01">
                                <ul class="navbar-nav mr-auto">
                                    <li class="nav-item">
                                    <a class="nav-link" href="{{path ('categorie')}}"></a>
                                    </li>
                                    
                                </ul>                   
                            
                                <ul class="navbar-nav">
                                        <li class="nav-item"><span class="caret">
                                        <a class="nav-link" href={{path ('account_register')}}>Inscription</a></span>
                                        </li> 

                                        <li class="nav-item"><span class="caret">
                                        <a class="nav-link" href={{path ('account_login')}}>Mon Compte !</a></span>
                                        </li>
                                </ul>
                            </div>
                        {% endif %}
                    

                    
                    
                </div>
</nav>
                {% for label, messages in app.flashes %}
                    <div class="container">
                    <div class="alert alert-{{ label }}">
                        {% for message in messages %}
                        <p>{{ message | raw }}</p>
                        {% endfor %}
                    </div>
                </div>
                {% endfor %}